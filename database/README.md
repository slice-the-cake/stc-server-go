# STC Database

The server uses [PostgreSQL](https://www.postgresql.org/) as the database.

The project is in very early stages. As such, it doesn't use proper database migrations yet. The evolving `schema.sql` file contains the latest schema. At some point migrations will be introduced, likely
favoring a barebones, simple approach.

